# Repository Instructions

## Update Log

- 每次完成任何功能更新、功能修改、行为调整或缺陷修复后，必须在结束任务前将本次变更写入仓库根目录的 `UpdateLog.md`。
- 更新记录应至少包含日期、涉及的功能以及具体变更内容；执行过验证时，也应简要记录验证结果。
- 新记录应添加到 `UpdateLog.md` 顶部，并保留已有历史记录，不得覆盖或删除旧日志。
