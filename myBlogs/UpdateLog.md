## 更新摘要（2026-09-05）：调整主页音量控件布局

- 主页音量图标向左移动最多 24px，窄卡片自动减小位移，避免挤占下一首按钮。
- 音量滑杆改为图标正上方居中展开的竖直滑杆，底部为低音量、顶部为高音量，保留静音和键盘操作。
- 同步前端、控制台及干净源码；TypeScript 检查通过，浏览器验证布局与向上键增加音量。

## 更新摘要（2026-09-05）：主页播放器音量控制

- 在主页 Cloud Music 播放器“下一首”右侧增加音量按钮，点击展开滑杆，可调节音量、静音/取消静音，支持双击音量按钮切换静音。
- 复用 MusicProvider 的音量与静音状态，与音乐页保持同步；拦截音量控件点击，避免触发播放器卡片跳转。
- 补充控件无障碍名称、展开状态和音量百分比，支持键盘调节、Esc 收起与焦点恢复；同步前端、控制台和干净源码中的 CloudPlayer。
- 验证：前端与控制台 TypeScript 检查通过；定向 ESLint 无错误（保留原有封面 img 提示）；浏览器验证滑杆、静音/取消静音、Esc 收起和操作后保持主页。

## 更新摘要（2026-09-05）

#### 照片墙渐进加载

- 博客照片墙的相册封面、叠放照片、搜索结果、相册照片和全屏查看接入 Next.js Image；预先生成的小尺寸高斯模糊 WebP 随页面发送，原图下载解码完成后移除占位，复用浏览器 HTTP 缓存。
- 新增构建/开发启动前预览生成脚本，支持远程与 public 本地图片、EXIF 方向、四并发、下载超时与重试、大小限制、远程七天缓存和失败降级；移除旧照片的缓存条目。
- 修复相册封面定位冲突，保持封面比例；搜索结果使用图片地址区分节点，避免切换结果时复用旧图片状态。
- 同步干净源码、依赖锁文件、更新器脚本清单和使用说明；生成的个人预览缓存不纳入 Git，并纳入干净仓库隐私路径检查。
- 验证：预览生成自动测试通过（方向、尺寸、去重、缓存、失败降级、清理与损坏缓存重建）；TypeScript 与生产构建通过；定向 ESLint 无错误，保留原有两条警告。浏览器确认加载中存在模糊预览，原图加载后移除占位，缓存照片全屏查看与封面布局正常。构建仍有 tree 页面既有文件追踪警告。

# yukiBlogs 更新日志

## 2026-08-20

### EdgeOne 部署与访客体验

- 主站支持迁移到腾讯云 EdgeOne Pages 并绑定自定义域名。
- 移除首页天气组件、`/api/weather` 路由和 `QWEATHER_KEY` 示例配置，避免访客反复遇到浏览器定位权限弹窗。
- 控制台部署文案切换为 EdgeOne 源码同步，减少从旧 Vercel 流程迁移后的误导。

## 2026-08-19

### 统计一致性

- 访客初始化、每日去重和累计递增改为 Redis Lua 原子操作。
- 旧版文章阅读键会自动迁移到统一的内容统计键，已有阅读数不再归零。
- 统计接口统一为 `/api/stats/view/[kind]/[slug]`，明确区分文章、杂谈和说说。
- 修复说说快速点击时首次计数请求被初始读取请求吞掉的问题。

### 展示修复

- 无封面的杂谈详情页也会正常记录并显示浏览次数。
- 恢复首页底部状态卡两行网格布局，避免中等屏宽下备案信息被裁切。
- 恢复中文 README，并同步中英文统计规则。

> 站长排除只阻止后续新增记录；启用排除前已经产生的累计数按当前约定保留。

## 2026-08-18

### 访问统计

- 接入 Upstash Redis 保存累计访客与内容阅读次数。
- 累计访客显示在桌面端顶部导航栏（`yukiの宝藏之地` 右侧），单个用户当天访问 +1，排除站长与常见机器人。
- 文章、杂谈、说说卡片封面的右下角展示浏览次数（单个用户当天浏览 +1）；文章/杂谈详情页打开时计数，说说在点开图片或评论时计数。
- 新增 `/stats-owner` 站长浏览器排除页与常见机器人过滤。

### 数据同步与稳定性

- 统一前端运行时配置读取方式。
- 修复友链、个人资料、页脚技术栈、建站时间和 AI 设置保存后仍显示旧数据的问题。
- 修复项目、相册及杂谈数据更新或删除后的残留显示。
- 加强删除结果判定、幂等重试和生成文件原子写入。

### 启动与部署

- Windows `Start.bat` 优先调用 uv 创建的 `.venv\Scripts\python.exe`。
- 补充 Gemini、天气、GitHub OAuth、Upstash Redis 与站长排除所需的服务端环境变量说明。
- 更新技术栈标识为 Next.js 16、React 19 与 Tailwind CSS 4。

## 0.2.1–0.2.3

- 优化移动端特效与页面适配。
- 新增关于页动态记录。
- 支持修改友链介绍。
- 修复启动脚本闪退与标题无法修改。
- 增加源码同步更新流程。

## 0.1.1

- 支持自定义网页标题与图标。
- 支持 LaTeX 公式。
- 修复评论系统数据读取。
- 扩展站点配置和环境变量。
