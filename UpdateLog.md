## 更新摘要（2026-09-05）：调整主页音量控件布局

- 主页音量图标向左移动最多 24px，窄卡片自动减小位移，避免挤占下一首按钮。
- 音量滑杆改为图标正上方居中展开的竖直滑杆，底部为低音量、顶部为高音量，保留静音和键盘操作。
- 同步前端、控制台及干净源码；TypeScript 检查通过，浏览器验证布局与向上键增加音量。

## 更新摘要（2026-09-05）：修复清理草稿误删已发布文章

- 删除接口按 draft/post/chatter 明确划分目标；未传类型时仅删除草稿，不再同时删除同 ID 的文章、杂谈。草稿页显式传入 draft，文章管理页传入 post，杂谈页沿用 chatter。
- 拒绝非法参数，文件删除失败不再伪报成功；发布内容的前端同步删除失败时返回明确提示，并允许安全重试。
- 验证：控制台 TypeScript 检查、5 项草稿同步测试与 4 项内容同步测试通过，覆盖同 ID 内容隔离、默认草稿删除、无效输入、权限失败和同步重试；个人文章未纳入干净源码。

## 内容恢复记录（2026-09-05）

- 排查 SQL 注入系列文章缺失：Git 记录显示 SQL 注入 03 在前端仓库 4b39698（20:36:39 自动同步源码）中被删除；现有构建和预览生成脚本未发现删除文章步骤，删除触发操作尚待确认。
- 将 SQL 注入 03 从控制台旧 standalone 副本恢复到本地控制台及博客前端 posts 目录，未覆盖任何现存文章；原副本及相关 Git 历史内容备份到 .codex-artifacts/article-recovery-20260905，恢复文件逐字节校验一致。
- SQL 注入 04 暂未在当前文章、草稿、旧构建副本及前端 Git 历史中找到；待确认是否完成发布队列执行。未执行构建、提交或推送，未向干净源码仓库写入个人文章。

## 更新摘要（2026-09-05）：主页播放器音量控制

- 在主页 Cloud Music 播放器“下一首”右侧增加音量按钮，点击展开滑杆，可调节音量、静音/取消静音，支持双击音量按钮切换静音。
- 复用 MusicProvider 的音量与静音状态，与音乐页保持同步；拦截音量控件点击，避免触发播放器卡片跳转。
- 补充控件无障碍名称、展开状态和音量百分比，支持键盘调节、Esc 收起与焦点恢复；同步前端、控制台和干净源码中的 CloudPlayer。
- 验证：前端与控制台 TypeScript 检查通过；定向 ESLint 无错误（保留原有封面 img 提示）；浏览器验证滑杆、静音/取消静音、Esc 收起和操作后保持主页。

## 更新摘要（2026-09-05）

#### 照片墙渐进加载

- 博客照片墙的相册封面、叠放照片、搜索结果、相册照片和全屏查看接入 Next.js Image；预先生成的小尺寸高斯模糊 WebP 随页面发送，原图下载解码完成后移除占位，复用浏览器 HTTP 缓存。
- 新增构建/开发启动前预览生成脚本，支持远程与 public 本地图片、EXIF 方向、四并发、下载超时与重试、大小限制、远程七天缓存和失败降级；移除旧照片的缓存条目。
- 修复相册封面定位冲突，保持封面比例；搜索结果使用图片地址区分节点，避免切换结果时复用旧图片状态。
- 同步干净源码、依赖锁文件、更新器脚本清单和使用说明；生成的个人预览缓存不纳入 Git，并纳入干净仓库隐私路径检查。
- 验证：预览生成自动测试通过（方向、尺寸、去重、缓存、失败降级、清理与损坏缓存重建）；TypeScript 与生产构建通过；定向 ESLint 无错误，保留原有两条警告。浏览器确认加载中存在模糊预览，原图加载后移除占位，缓存照片全屏查看与封面布局正常。构建仍有 tree 页面既有文件追踪警告。

## 更新摘要（2026-08-31）

#### 1. 增加项目级更新日志规则

> 新增根目录 `AGENTS.md`，规定每次完成任何功能更新、修改、行为调整或缺陷修复后，都必须在任务结束前同步记录到 `UpdateLog.md`，并保留已有历史日志。

## 更新摘要（2026-08-19）

#### 1. 修复统计并发一致性

> 访客迁移、每日去重和累计递增改为 Redis Lua 原子操作，避免首批并发或中途失败造成覆盖、漏计。

#### 2. 迁移旧版文章阅读数

> 统一使用 `/api/stats/view/[kind]/[slug]` 统计文章、杂谈和说说；旧版文章阅读键会在首次读取时自动合并一次。

#### 3. 修复浏览次数交互

> 无封面的杂谈现在也会正常计数；说说在初始读取尚未完成时快速点开图片或评论，计数请求会排队执行。

#### 4. 修复首页中等屏宽布局

> 恢复底部状态卡两行网格布局，避免技术栈徽章和备案信息在 800px 左右宽度下被裁切。

#### 5. 同步文档和完整源码

> 恢复部署仓库中文 README，同步中英文统计规则，并将本地控制台、内容和前端最新源码归档到私有完整源码仓库。

> 站长排除只阻止后续新增记录；启用排除前已经产生的累计数按当前约定保留。

## 更新摘要（2026-08-18）

#### 1. 新增累计访客与文章阅读统计

> 接入 Upstash Redis 持久化统计；累计访客移动至桌面端顶部导航栏，文章详情页显示阅读次数，并增加十分钟事件去重。

#### 2. 新增站长统计排除

> 新增 `/stats-owner` 私有控制页。配置 `STATS_OWNER_KEY` 后可排除当前站长浏览器，同时过滤常见机器人请求。

#### 3. 修复控制台数据同步链路

> 统一前台运行时读取方式，修复友链、个人资料、页脚技术栈、建站时间、AI 设置、项目/相册和杂谈内容更新后仍显示旧数据的问题。

#### 4. 修复内容删除及写入稳定性

> 加强删除结果判定、幂等重试与生成文件原子写入，减少删除后残留卡片、重复执行和半写入文件。

#### 5. 修复 Windows 启动脚本

> `Start.bat` 优先调用 uv 创建的 `.venv\Scripts\python.exe`，并保留可见错误信息，避免启动失败后窗口直接关闭。

#### 6. 更新技术栈和密钥说明

> README 已同步 Next.js 16、React 19、Tailwind CSS 4，以及 Gemini、天气、GitHub OAuth、Upstash Redis 和站长统计排除所需的服务端环境变量。

## 更新日志 (版本0.2.1~版本0.2.3)

#### 1. 优化在移动端显示卡顿情况

> 调整在移动端显示特效，减轻移动端卡顿情况

#### 2. 增加各个页面对移动端适配

> 实现首页、杂谈、归档等页面的移动端适配，增加移动端导航栏。

#### 3. 关于新增动态

> 可以看到提交记录

#### 4. 友链介绍可修改

> 现在你可以在控制台中编辑可复制信息了

#### 5. 存在的几个bug修复

> Start.dat闪退，标题无法修改问题已修复

#### 6. 更新博客源码

> 实现更新源码

## 更新日志 (版本0.1.1)

#### 1. 标题页与图标更新

> 实现自定义网页标题和图标按钮，详细见设置中的个人名片设置

#### 2. 文章支持Latex格式

> 实现Latex格式公式显示，具体使用如
>
> 输入公式如 $ E = mc^2 $ 下划线需使用转义符"\\_"。
>
> 此处感谢同学支持更新Latex公式显示 @inWunsch

#### 3. 评论系统bug修复

> 修复评论系统无法正常获取bug

#### 4. siteconfig设置新增

> 新增faviconUrl、navTitle等多项，更新同步拉取代码时请注意保存之前信息！！

#### 5. 环境变量

> 所需环境变量增多，请注意！
## 更新摘要（2026-09-09）：修复 P1 数据安全与发布可靠性问题

- 管理端内容保存与发布统一校验文档 ID，拒绝路径穿越；新草稿和新文章使用毫秒时间戳加随机后缀，避免同刻覆盖。
- 同步目录改为临时目录复制后替换，并保留失败前的旧目录，避免复制失败导致镜像被清空。
- 发布操作队列增加 localStorage 持久化和恢复，减少刷新或窗口退出造成的待发布操作丢失。
- LX 音源脚本在执行前完成 SHA-256 校验；公开配置生成排除嵌套图床配置；更新器补齐前后端 lib 目录。
- 管理端锁定 sharp 0.35.x 以上版本以避开已知原生图片解码风险。
- 验证：运行管理端 TypeScript 检查与定向 Python 测试；完整测试仍需继续覆盖同步故障回滚及队列恢复场景。
